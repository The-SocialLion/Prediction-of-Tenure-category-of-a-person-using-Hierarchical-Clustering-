# -*- coding: utf-8 -*-
"""Untitled49.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1gdkX0ERcXU48UAwwG409JCtAgXCqvBKB
"""

import numpy as np
import matplotlib.pyplot as plt
import pandas as pd

l_map={
    6:0,
    7:1,
    8:2,
    9:3,
    10:4,
    11:5,
    12:6
}

df=pd.read_csv('CC.csv')
df=df.dropna(how='any')
df=df.drop(columns=['CUST_ID'])
df['TENURE']=df['TENURE'].map(l_map)
display(df)

X=df.iloc[:,:-1].values
Y=df.iloc[:,-1].values

import scipy.cluster.hierarchy as sch
dend=sch.dendrogram(sch.linkage(X,method='ward'))
plt.title('Dendrogram')
plt.xlabel('Customers')
plt.ylabel('Euclidean distances')
plt.show()

from sklearn.cluster import AgglomerativeClustering
hc = AgglomerativeClustering(n_clusters = 20, affinity = 'euclidean', linkage = 'ward')
y_pred = hc.fit_predict(X)

y_pred

from sklearn import metrics
metrics.adjusted_rand_score(Y,y_pred)

metrics.adjusted_mutual_info_score(Y,y_pred)